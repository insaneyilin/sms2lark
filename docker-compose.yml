services:
  vcode-agent:
    build: .
    ports:
      - "8900:8900"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8900/health"]
      interval: 30s
      timeout: 5s
      retries: 3
